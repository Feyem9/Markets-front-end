{
  "build": {
    "env": {
      "NPM_FLAGS": "--legacy-peer-deps"
    },
    "rewrites": [
      {
        "source": "/(.*)",
        "destination": "/index.html"
      }
    ],
    "use": "@vercel/static-build",
    "config": {
      "distDir": "dist/market"
    }
  }
}